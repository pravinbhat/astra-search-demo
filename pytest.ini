[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
markers =
    skip: Skip test
    asyncio: Mark test as async
asyncio_mode = auto

; Made with Bob
